# A00 · 设计理念与架构总览 — 归档快照

这是系列文章 **第 00 篇（导论）** 结束时的独立项目快照。

## 说明

- 本篇为导论，**无代码**，仅包含项目骨架与文章文档。
- 如需查看最新开发版本，请回到根目录 `deerflow-harness/`。

## 运行

请回到仓库根目录 `deerflow-harness/` 执行（共享根目录的 `.venv`）：

```bash
PYTHONPATH=archive/a00-overview/src uv run pytest archive/a00-overview/tests -q
```

（A00 无实际代码，测试仅包含一个占位用例。）

## 与根目录的关系

- `archive/a00-overview/`：A00 结束时的状态（无代码）
- `deerflow-harness/`（根目录）：最新开发版本，继续承载 A03+ 的代码
